<h1 align="center">All users</h1><br />
<div class="apple_pagination">
<%= will_paginate @users, :container => false %>
</div>
<div class="span12" id="innerbox">

		<ul class="users">
			<% @users.each do |user| %>
				<li>
					<%= render user %>
				</li>
			<% end %>
		</ul>
</div>
<div class="apple_pagination">
<%= will_paginate @users, :container => false %>
</div>